<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dorner.csgomapscallouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ItalyActivity" />
        <activity android:name=".activities.CacheActivity" />
        <activity android:name=".activities.CobbleActivity" />
        <activity android:name=".activities.DustActivity" />
        <activity android:name=".activities.Dust2Activity" />
        <activity android:name=".activities.InfernoActivity" />
        <activity android:name=".activities.MirageActivity" />
        <activity android:name=".activities.NukeActivity" />
        <activity android:name=".activities.OverpassActivity" />
        <activity android:name=".activities.SeasonActivity" />
        <activity android:name=".activities.TrainActivity"></activity>
    </application>

</manifest>